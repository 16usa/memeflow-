Copy native-ai-bootstrap.mjs and native-ai-sheet.js into memeflow-app/.
Replace memeflow-app/package.json with the included package.json.
Set OPENAI_API_KEY in Replit Secrets. Optional: OPENAI_MODEL (defaults to gpt-5).
Then restart the Replit app.
