#!/usr/bin/env node
import fs from 'fs';import path from 'path';import {spawnSync} from 'child_process';const root=process.cwd();const target=[path.join(root,'memeflow-app','index.html'),path.join(root,'index.html'),path.join(root,'artifacts','memeflow','index.html')].find(p=>fs.existsSync(p));if(!target){console.error('V36 VERIFY: index.html not found.');process.exit(1)}const appDir=path.dirname(target);const html=fs.readFileSync(target,'utf8');const rp=path.join(appDir,'ai-sparkles-icon-v36.js');const rt=fs.existsSync(rp)?fs.readFileSync(rp,'utf8'):'';const checks=[['V36 tag once',(html.match(/ai-sparkles-icon-v36\.js\?v=36\.0\.0/g)||[]).length===1],['runtime exists',fs.existsSync(rp)],['runtime syntax',fs.existsSync(rp)&&spawnSync(process.execPath,['--check',rp],{encoding:'utf8'}).status===0],['three sparkle paths',(rt.match(/%3Cpath/g)||[]).length>=3],['existing button is not replaced',!/replaceWith\(|outerHTML\s*=/.test(rt)],['no click handler replacement',!/onclick\s*=|addEventListener\(['"]click/.test(rt)],['no API calls',!/fetch\(|XMLHttpRequest|\/api\//.test(rt)],['targets mobile nav',/\.mobile-nav/.test(rt)]];console.log('=== MEMEFLOW V36 VERIFY ===');for(const [l,o] of checks)console.log(`${o?'PASS':'FAIL'}  ${l}`);const f=checks.filter(([,o])=>!o);if(f.length){console.error(`V36 VERIFY FAILED: ${f.length}`);process.exit(1)}console.log('V36 VERIFY OK: 8/8');
