import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {spawnSync} from 'node:child_process';const root='/home/runner/workspace',app=path.join(root,'memeflow-app'),pkg=path.join(root,'pepe-game-v8.3'),expected={"game.html": "a1bfa869ed429be15b684b096f53ab5ecb156c37e2e989264c47db41b756d741", "game.css": "5fdc96fbdd7deeb02dd57544851ba7ff001a465ab0af64cd615b646bc8ea401d"},baseline={"game.html": "02f4d0c0f0363c1f72c98a83ff798a2416a01230d3b7d70b2b71239a51e41e70", "game.css": "7e3a1fe168858b22f2d9c535f0e952cb4c03392bf55d3b92af75dda10abea7c0", "game.js": "303a31dd3084d65ce375f85f0cc50555ceeab0d12196100dd1381fcede225575"};const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');let n=0;const pass=(x,o)=>{if(!o)throw new Error('FAIL '+x);n++;console.log('PASS',x)};for(const [f,h] of Object.entries(expected))pass('V8.3 hash '+f,sha(path.join(app,f))===h);pass('game.js unchanged',sha(path.join(app,'game.js'))===baseline['game.js']);const h=fs.readFileSync(path.join(app,'game.html'),'utf8'),c=fs.readFileSync(path.join(app,'game.css'),'utf8');pass('cache bust',h.includes('/game.css?v=83')&&h.includes('/game.js?v=83'));pass('iOS no-focus-zoom viewport',h.includes('maximum-scale=1')&&h.includes('user-scalable=no'));pass('touch controls 16px',c.includes('input,select,textarea{font-size:16px!important'));pass('multiplier x class',h.includes('class="multiplier-x">×</span>')&&c.includes('.multiplier>.multiplier-x{display:inline-block!important'));pass('duplicate Flight Plan hidden',/\.risk-deck\{[^}]*display:none/.test(c));pass('duplicate Flight Record hidden',/\.stats-panel\{[^}]*display:none/.test(c));pass('duplicate desktop position strip hidden',c.includes('.position-strip{display:none}'));pass('one-screen desktop',c.includes('grid-template-rows:minmax(0,1fr) 82px'));pass('CSS braces balanced',(c.match(/\{/g)||[]).length===(c.match(/\}/g)||[]).length);pass('JS syntax',spawnSync(process.execPath,['--check',path.join(app,'game.js')]).status===0);pass('protected engine exists',fs.existsSync(path.join(app,'src','game-engine.mjs')));pass('protected server exists',fs.existsSync(path.join(app,'app-server.mjs')));pass('protected index exists',fs.existsSync(path.join(app,'index.html')));pass('payload HTML/CSS only',fs.readdirSync(path.join(pkg,'payload')).sort().join(',')==='game.css,game.html');console.log(`PEPE GAME V8.3 VERIFY: PASS (${n} checks)`);