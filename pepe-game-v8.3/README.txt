PEPE ROCKET V8.3 — TOUCH LOCK + MULTIPLIER + DEDUP

Exact baseline: V8.2.
Fixes iPhone focus/double-tap zoom behavior, makes × explicit, removes duplicate visual sections, and consolidates desktop one-screen CSS. game.js/trading/server unchanged.
