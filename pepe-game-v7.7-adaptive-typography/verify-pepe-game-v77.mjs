import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {spawnSync} from 'node:child_process';
const root='/home/runner/workspace',app=path.join(root,'memeflow-app'),pkg=path.join(root,'pepe-game-v7.7-adaptive-typography'),expected={"game.html": "4b618f377f0563a68e1e1a095cdf2180a049bc1be72daaa2869a6b768102db86", "game.css": "6b4a6b10d4378c52d7739078e3a346c46d7bc8f4d03046ebaebe62a120505459", "game.js": "04c03223736d197f1d89a2b8749fa9222e9ceac46c27c9415171fb01ea8a3509"};const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');let n=0;const pass=(x,o)=>{if(!o)throw new Error('FAIL '+x);n++;console.log('PASS',x)};
for(const [f,h] of Object.entries(expected))pass('V7.7 hash '+f,sha(path.join(app,f))===h);
const h=fs.readFileSync(path.join(app,'game.html'),'utf8'),c=fs.readFileSync(path.join(app,'game.css'),'utf8');
pass('cache V7.7',h.includes('/game.css?v=77adaptive'));pass('portrait readable type',c.includes('font-size:clamp(7px,1.7vw,10px)'));pass('launch uses flex',c.includes('.launch-panel{grid-column:1;grid-row:2/5;height:100%;padding:4px;display:flex;flex-direction:column'));pass('stake fixed compact height',c.includes('.stake-input{flex:0 0 27px;height:27px'));pass('desktop adaptive columns',c.includes('grid-template-columns:minmax(260px,18vw) minmax(0,1fr) minmax(300px,20vw)'));pass('no page scroll',c.includes('overflow:hidden!important;overscroll-behavior:none'));pass('JS syntax',spawnSync(process.execPath,['--check',path.join(app,'game.js')]).status===0);pass('runtime smoke',spawnSync(process.execPath,[path.join(pkg,'runtime-smoke-v77.cjs'),app],{stdio:'inherit'}).status===0);pass('protected engine',fs.existsSync(path.join(app,'src','game-engine.mjs')));pass('protected server',fs.existsSync(path.join(app,'app-server.mjs')));pass('protected index',fs.existsSync(path.join(app,'index.html')));pass('payload client-only',fs.readdirSync(path.join(pkg,'payload')).sort().join(',')==='game.css,game.html,game.js');console.log(`PEPE GAME V7.7 VERIFY: PASS (${n} checks)`);
