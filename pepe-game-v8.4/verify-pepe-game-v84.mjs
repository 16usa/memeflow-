import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {spawnSync} from 'node:child_process';const root='/home/runner/workspace',app=path.join(root,'memeflow-app'),pkg=path.join(root,'pepe-game-v8.4'),expected={"game.html": "458c3c68559915bdef17c78fd2dc8eb53566b202e4633b208e6d0371401464a3", "game.css": "864c9d1c30a9b1fd263fd2c4b25b67eafeb94fb8ce649eb18e3692cac50b9e6e"},baseline={"game.html": "a1bfa869ed429be15b684b096f53ab5ecb156c37e2e989264c47db41b756d741", "game.css": "5fdc96fbdd7deeb02dd57544851ba7ff001a465ab0af64cd615b646bc8ea401d", "game.js": "303a31dd3084d65ce375f85f0cc50555ceeab0d12196100dd1381fcede225575"};const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');let n=0;const pass=(x,o)=>{if(!o)throw new Error('FAIL '+x);n++;console.log('PASS',x)};for(const [f,h] of Object.entries(expected))pass('V8.4 hash '+f,sha(path.join(app,f))===h);pass('game.js unchanged',sha(path.join(app,'game.js'))===baseline['game.js']);const h=fs.readFileSync(path.join(app,'game.html'),'utf8'),c=fs.readFileSync(path.join(app,'game.css'),'utf8');pass('cache bust',h.includes('/game.css?v=84')&&h.includes('/game.js?v=84'));pass('no zoom viewport kept',h.includes('maximum-scale=1')&&h.includes('user-scalable=no'));pass('mobile page overflow locked',c.includes('html,body{height:100%;min-height:0;overflow:hidden'));pass('mobile dock removed',c.includes('.mode,.network-strip,.sound,.footer,.mobile-dock,.stats-panel{display:none!important'));pass('READY multiplier visible',c.includes('.game[data-state="idle"] .multiplier-hud,.game[data-state="searching"] .multiplier-hud{opacity:1!important'));pass('multiplier x explicit',h.includes('class="multiplier-x">×</span>'));pass('mobile one-screen grid',c.includes('grid-template-rows:minmax(225px,38%) minmax(215px,35%) 46px minmax(82px,1fr)'));pass('history compact 4',c.includes('.history-row:nth-child(n+5){display:none}'));pass('duplicate risk hidden',c.includes('.risk-deck{display:none!important}'));pass('JS syntax',spawnSync(process.execPath,['--check',path.join(app,'game.js')]).status===0);pass('protected engine exists',fs.existsSync(path.join(app,'src','game-engine.mjs')));pass('protected server exists',fs.existsSync(path.join(app,'app-server.mjs')));pass('protected index exists',fs.existsSync(path.join(app,'index.html')));pass('payload HTML/CSS only',fs.readdirSync(path.join(pkg,'payload')).sort().join(',')==='game.css,game.html');console.log(`PEPE GAME V8.4 VERIFY: PASS (${n} checks)`);