MEMEFLOW AI DUPLICATE MODAL FIX V28 — SELF TEST

PASS 100 / 100 installer/idempotency simulations

Verified:
- installer changes only one V28 script tag
- V24 AI runtime remains untouched
- V26 layout remains untouched
- V27 wallet layout remains untouched
- native #sheet-ai-direct-v24 is explicitly excluded from hiding
- legacy UI detection requires MEMEFLOW OpenAI + input + textarea + Ask AI
- legacy backend DOM is hidden, never deleted
- V28 has no fetch/API/evaluator code
- active modal guard runs only while the native sheet is open
