PEPE REAL RIG V16.1 — VIEWPORT / CENTERING FIX

Fixes:
- giant cropped character on iPhone
- character shifted right/down
- responsive fit on portrait screens
- keeps full body visible between HUD and buttons

Install:
cd ~/workspace
unzip -o PEPE_REAL_RIG_V16_1_VIEWPORT_FIX.zip
bash PEPE_REAL_RIG_V16_1_VIEWPORT_FIX/install.sh

Open:
/character-real-test-v16.html?refresh=1610

This does NOT change the V16 rig or PNG assets.
It only replaces the V16 test renderer/camera.
