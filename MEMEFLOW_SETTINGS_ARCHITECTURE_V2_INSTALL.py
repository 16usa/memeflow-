#!/usr/bin/env python3
from pathlib import Path
import subprocess, re

MARK='MEMEFLOW_SETTINGS_ARCHITECTURE_V2'
root=Path.cwd()
if (root/'memeflow-app').is_dir(): app=root/'memeflow-app'
elif (root/'app-server.mjs').is_file() and (root/'src').is_dir(): app=root
else: raise SystemExit('ERROR: memeflow-app not found. Run from the Replit project root.')
paths={
 'gate':app/'src'/'settings-gate.mjs',
 'settings':app/'src'/'settings.mjs',
 'page':app/'settings-page.js',
 'html':app/'settings.html',
 'pkg':app/'package.json',
 'test':app/'tests'/'settings-architecture-v2.mjs',
 'app':app/'app-server.mjs'
}
for k in ('gate','settings','page','html','pkg','app'):
    if not paths[k].exists(): raise SystemExit(f'ERROR: missing {paths[k]}')

def run(cmd,cwd=None):
    print('+',' '.join(map(str,cmd)))
    subprocess.run(cmd,cwd=cwd,check=True)

def replace_once(text,old,new,label):
    n=text.count(old)
    if n!=1: raise RuntimeError(f'PATCH ERROR [{label}]: expected exactly 1 anchor, found {n}')
    return text.replace(old,new,1)

def sub_once(text,pattern,repl,label,flags=re.S):
    out,n=re.subn(pattern,repl,text,count=1,flags=flags)
    if n!=1: raise RuntimeError(f'PATCH ERROR [{label}]: expected exactly 1 regex anchor, found {n}')
    return out

touch_keys=('gate','settings','page','html','pkg')
rel=[str(paths[k].relative_to(root)) for k in touch_keys]
if paths['test'].exists(): rel.append(str(paths['test'].relative_to(root)))
dirty=subprocess.run(['git','status','--porcelain','--',*rel],cwd=root,text=True,capture_output=True,check=True).stdout.strip()
if dirty:
    print('ERROR: target files already have local changes:')
    print(dirty)
    print('Nothing was changed.')
    raise SystemExit(1)

originals={k:paths[k].read_text(encoding='utf-8') for k in touch_keys}
test_existed=paths['test'].exists()
test_original=paths['test'].read_text(encoding='utf-8') if test_existed else None

NEW_GATE_BLOCK="// MEMEFLOW_STRICT_ENTRY_ADMISSION_V1\n// MEMEFLOW_SETTINGS_ARCHITECTURE_V2\n//\n// Settings are intentionally split into three non-overlapping policy layers:\n//\n// 1) ENTRY_ADMISSION_KEYS\n//    Decide whether a token is visible in Live Token States.\n//    FAIL/WAITING => hidden lightweight PRE-ADMISSION telemetry.\n//\n// 2) LOGIC_DECISION_KEYS\n//    Run only AFTER admission. They may produce WAITING / WATCH / BUY READY,\n//    but they must never hide an otherwise admitted token from the feed.\n//\n// 3) PREOPEN_RPC_KEYS\n//    Heavy linked/funded-wallet verification. These are FINAL-ONLY and remain\n//    behind BUY READY. They never participate in discovery/admission.\nexport const ENTRY_ADMISSION_KEYS = Object.freeze([\n  'launchPlatforms',\n  'includeKeywords',\n  'excludeKeywords',\n\n  'minBondingCurvePct',\n  'maxBondingCurvePct',\n  'minMarketCapUsd',\n  'maxMarketCapUsd',\n  'minTotalFeesSol',\n  'maxTotalFeesSol',\n  'minVolume24hUsd',\n  'maxVolume24hUsd',\n  'minBuyTransactions',\n  'maxBuyTransactions',\n  'minSellTransactions',\n  'maxSellTransactions',\n  'minTotalTransactions',\n  'maxTotalTransactions',\n  'minHolders',\n  'maxHolders',\n  'minBundlePct',\n  'maxBundlePct',\n  'minTokenAgeMinutes',\n  'maxTokenAgeMinutes',\n  'minTop10Pct',\n  'maxTop10Pct',\n  'minDeveloperPct',\n  'maxDeveloperPct',\n  'minSniperPct',\n  'maxSniperPct',\n  'minLiquidityUsd',\n\n  'developerBlacklistWallets',\n  'requireTwitter',\n  'requireWebsite',\n  'requireTelegram',\n  'requireAnySocial'\n]);\n\nexport const LOGIC_DECISION_KEYS = Object.freeze([\n  'minScore',\n  'minConfidence',\n  'minBuyPressure',\n  'decisionFreshnessSec',\n  'requireFreshHolderSnapshot',\n  'requireWebsiteOrX'\n]);\n\nexport const PREOPEN_RPC_KEYS = Object.freeze([\n  'maxSuspectedRiskyWalletsPct',\n  'maxInsidersPct'\n]);\n\nconst ENTRY_ADMISSION_KEY_SET = new Set(ENTRY_ADMISSION_KEYS);\n\nexport function evaluateEntryAdmission(token={},settings={},options={}){\n  const now=Number(options?.now)||Date.now();\n  void now;\n\n  // evaluateSettingsGate is CPU-only. We intentionally reuse its canonical\n  // metric readers, then keep ONLY real Entry Filter gates.\n  const full=evaluateSettingsGate(token,settings);\n\n  const gates=(full.gates||[])\n    .filter(g=>ENTRY_ADMISSION_KEY_SET.has(String(g?.key||'')));\n\n  const failedGates=gates.filter(g=>g?.status==='FAIL');\n  const waitingGates=gates.filter(g=>g?.status==='WAITING');\n\n  const hasStableFailure=failedGates.some(g=>g?.retryable!==true);\n  const hasRetryableFailure=failedGates.some(g=>g?.retryable===true);\n  const admitted=failedGates.length===0&&waitingGates.length===0;\n\n  let state='ADMITTED';\n  if(!admitted){\n    state=hasStableFailure?'REJECTED':'PENDING';\n  }\n\n  return {\n    admitted,\n    state,\n    gates,\n    failedGates,\n    waitingGates,\n    hasStableFailure,\n    hasRetryableFailure,\n    entryKeys:ENTRY_ADMISSION_KEYS,\n    logicKeys:LOGIC_DECISION_KEYS,\n    finalOnlyKeys:PREOPEN_RPC_KEYS,\n    reasons:[...failedGates,...waitingGates]\n      .map(g=>g?.reason)\n      .filter(Boolean)\n  };\n}\n\nexport function isEntryAdmitted(token={},settings={},options={}){\n  return evaluateEntryAdmission(token,settings,options).admitted===true;\n}\n"
NEW_SETTINGS_ADMISSION="export function evaluateSettingsAdmission(token={},entries=[],options={}){\n  const now=Number(options.recheckMs)||5000;\n  void now;\n  const at=Number(options.now)||Date.now();\n  const recheckMs=Math.max(1000,Number(options.recheckMs)||5000);\n  const context=Array.isArray(entries)?entries:[];\n  const signature=settingsContextSignature(context);\n\n  if(context.length===0){\n    return {\n      allow:true,\n      drop:false,\n      reason:'no_active_users_fail_open',\n      signature,\n      users:0,\n      retryable:false,\n      recheckAt:null,\n      blockedUsers:0,\n      reasons:[]\n    };\n  }\n\n  const blocked=[];\n\n  for(const entry of context){\n    const gate=evaluateEntryAdmission(\n      token,\n      entry?.settings||{},\n      {now:at}\n    );\n\n    // At least one active user already passes every real Entry Filter.\n    if(gate.admitted===true){\n      return {\n        allow:true,\n        drop:false,\n        reason:'active_user_entry_filters_pass',\n        signature,\n        users:context.length,\n        retryable:false,\n        recheckAt:null,\n        blockedUsers:blocked.length,\n        uid:entry?.uid||null,\n        gate\n      };\n    }\n\n    // Missing data for an ENABLED Entry Filter must be allowed to warm.\n    // This preserves holder/market enrichment without letting Logic settings\n    // (buy pressure, confidence, website/X decision rule, etc.) reject raw WS.\n    if(\n      gate.state==='PENDING' &&\n      Array.isArray(gate.waitingGates) &&\n      gate.waitingGates.length>0\n    ){\n      return {\n        allow:true,\n        drop:false,\n        reason:'active_user_entry_data_pending',\n        signature,\n        users:context.length,\n        retryable:false,\n        recheckAt:null,\n        blockedUsers:blocked.length,\n        uid:entry?.uid||null,\n        gate\n      };\n    }\n\n    blocked.push({uid:entry?.uid||null,gate});\n  }\n\n  const failed=blocked.flatMap(row=>row.gate.failedGates||[]);\n\n  // Retry only if at least one user is blocked exclusively by market facts\n  // that can recover (MC/holders/concentration/etc.). Stable failures such as\n  // platform/keyword/blacklist can be safely discarded for that settings set.\n  const retryable=blocked.some(\n    row=>row.gate.hasStableFailure!==true\n  );\n\n  const reasons=[\n    ...new Set(\n      failed.map(g=>g.reason).filter(Boolean)\n    )\n  ].slice(0,8);\n\n  return {\n    allow:false,\n    drop:true,\n    reason:'entry_filters_rejected_for_all_active_users',\n    signature,\n    users:context.length,\n    retryable,\n    recheckAt:retryable?at+recheckMs:null,\n    blockedUsers:blocked.length,\n    reasons,\n    failedKeys:[\n      ...new Set(\n        failed.map(g=>g.key).filter(Boolean)\n      )\n    ].slice(0,16)\n  };\n}"
NEW_GROUPS="const MF293_GROUPS = [\n  ['logic', 'Logic', 'Post-admission decision rules · controls WAITING / WATCH / BUY READY', true, [\n    ['operatingMode', 'Operating mode', 'select', [['observe','Observe'],['assist','Assist'],['automate','Automate']]],\n    ['tradingEnvironment', 'Trading environment', 'select', [['paper','Paper'],['live','Live']]],\n    ['profile', 'Decision preset', 'select', [['conservative','Conservative'],['balanced','Balanced'],['aggressive','Aggressive'],['custom','Custom']]],\n    ['minScore', 'Minimum AI score', 'number', 0, 100, 1],\n    ['minConfidence', 'Minimum confidence %', 'number', 0, 100, 1],\n    ['minBuyPressure', 'Minimum buy pressure for BUY READY', 'number', 0, null, 0.01],\n    ['decisionFreshnessSec', 'Decision freshness sec', 'integer', 5, 3600, 1],\n    ['requireFreshHolderSnapshot', 'Require fresh holders for decision', 'boolean'],\n    ['requireWebsiteOrX', 'Require website or X for decision', 'boolean'],\n    ['shadowValidation', 'Shadow validation', 'boolean'],\n    ['changeLog', 'Settings change log', 'boolean']\n  ]],\n  ['trading', 'Trading', 'Capital, position sizing and daily limits', true, [\n    ['tradingCapital', 'Trading capital SOL', 'number', 0, null, 0.01],\n    ['dailySpendLimit', 'Daily spend limit SOL', 'number', 0, null, 0.01],\n    ['positionSize', 'Default position SOL', 'number', 0.000001, null, 0.01],\n    ['maxPositionSize', 'Maximum position SOL', 'number', 0.000001, null, 0.01],\n    ['maxOpenPositions', 'Maximum open positions', 'integer', 0, null, 1],\n    ['maxDailyEntries', 'Maximum daily entries', 'integer', 0, null, 1],\n    ['dailyLossLimit', 'Daily loss limit SOL', 'number', 0, null, 0.01],\n    ['feeReserve', 'Fee reserve SOL', 'number', 0, null, 0.001]\n  ]],\n  ['filters', 'Entry filters', 'Scanner admission only · failing tokens stay hidden in pre-admission', false, [\n    ['minLiquidityUsd', 'Minimum liquidity USD', 'number', 0, null, 1],\n    ['minHolders', 'Minimum holders', 'nullable', 0, null, 1],\n    ['maxHolders', 'Maximum holders', 'nullable', 0, null, 1],\n    ['minTokenAgeMinutes', 'Minimum age min', 'nullable', 0, null, 0.1],\n    ['maxTokenAgeMinutes', 'Maximum age min', 'nullable', 0, null, 0.1],\n    ['minMarketCapUsd', 'Minimum market cap USD', 'nullable', 0, null, 1],\n    ['maxMarketCapUsd', 'Maximum market cap USD', 'nullable', 0, null, 1],\n    ['minBondingCurvePct', 'Minimum bonding curve %', 'nullable', 0, 100, 0.1],\n    ['maxBondingCurvePct', 'Maximum bonding curve %', 'nullable', 0, 100, 0.1],\n    ['minTotalFeesSol', 'Minimum total fees SOL', 'nullable', 0, null, 0.001],\n    ['maxTotalFeesSol', 'Maximum total fees SOL', 'nullable', 0, null, 0.001],\n    ['minVolume24hUsd', 'Minimum 24h volume USD', 'nullable', 0, null, 1],\n    ['maxVolume24hUsd', 'Maximum 24h volume USD', 'nullable', 0, null, 1],\n    ['minBuyTransactions', 'Minimum buy transactions', 'nullable', 0, null, 1],\n    ['maxBuyTransactions', 'Maximum buy transactions', 'nullable', 0, null, 1],\n    ['minSellTransactions', 'Minimum sell transactions', 'nullable', 0, null, 1],\n    ['maxSellTransactions', 'Maximum sell transactions', 'nullable', 0, null, 1],\n    ['minTotalTransactions', 'Minimum total transactions', 'nullable', 0, null, 1],\n    ['maxTotalTransactions', 'Maximum total transactions', 'nullable', 0, null, 1],\n    ['minTop10Pct', 'Minimum Top 10 %', 'nullable', 0, 100, 0.1],\n    ['maxTop10Pct', 'Maximum Top 10 %', 'nullable', 0, 100, 0.1],\n    ['minDeveloperPct', 'Minimum developer %', 'nullable', 0, 100, 0.1],\n    ['maxDeveloperPct', 'Maximum developer %', 'nullable', 0, 100, 0.1],\n    ['minBundlePct', 'Minimum bundle %', 'nullable', 0, 100, 0.1],\n    ['maxBundlePct', 'Maximum bundle %', 'nullable', 0, 100, 0.1],\n    ['minSniperPct', 'Minimum sniper %', 'nullable', 0, 100, 0.1],\n    ['maxSniperPct', 'Maximum sniper %', 'nullable', 0, 100, 0.1],\n    ['requireTwitter', 'Require X / Twitter', 'boolean'],\n    ['requireWebsite', 'Require website', 'boolean'],\n    ['requireTelegram', 'Require Telegram', 'boolean'],\n    ['requireAnySocial', 'Require any social', 'boolean'],\n    ['includeKeywords', 'Include keywords', 'text'],\n    ['excludeKeywords', 'Exclude keywords', 'text'],\n    ['developerBlacklistWallets', 'Developer blacklist wallets', 'array']\n  ]],\n  ['preopen', 'Pre-open RPC verification', 'Only after BUY READY · linked and funded wallet risk', false, [\n    ['maxSuspectedRiskyWalletsPct', 'Maximum linked / risky wallets %', 'nullable', 0, 100, 0.1],\n    ['maxInsidersPct', 'Maximum insiders / common-funder wallets %', 'nullable', 0, 100, 0.1]\n  ]],\n  ['exits', 'Risk & exits', 'Stops, take profit allocation and exit pressure', true, [\n    ['hardStopPct', 'Hard stop %', 'number', 0.000001, 100, 0.1],\n    ['trailingStopPct', 'Trailing stop %', 'number', 0, 100, 0.1],\n    ['tp1Pct', 'TP1 gain %', 'number', 0.000001, null, 1],\n    ['tp1SellPct', 'TP1 sell %', 'number', 0, 100, 1],\n    ['tp2Pct', 'TP2 gain %', 'number', 0.000001, null, 1],\n    ['tp2SellPct', 'TP2 sell %', 'number', 0, 100, 1],\n    ['runnerPct', 'Runner %', 'number', 0, 100, 1],\n    ['maxHoldMinutes', 'Maximum hold min', 'integer', 1, null, 1],\n    ['exitBuyPressure', 'Exit buy pressure', 'number', 0, null, 0.01],\n    ['exitOnWeakBuyPressure', 'Exit on weak buy pressure', 'boolean']\n  ]]\n];"
PROFILE_HELPERS='\nfunction mf293PresetMatches(profile) {\n  const key = String(profile || \'\').trim().toLowerCase();\n  const preset = MF293.profilePresets?.[key];\n  if (!preset || typeof preset !== \'object\') return false;\n\n  for (const settingKey of MF293_PROFILE_LOGIC_KEYS) {\n    if (!Object.prototype.hasOwnProperty.call(preset, settingKey)) continue;\n    const input = document.querySelector(`[data-setting-key="${settingKey}"]`);\n    if (!input) return false;\n\n    const expected = preset[settingKey];\n    if (input.dataset.settingKind === \'boolean\') {\n      if (input.checked !== Boolean(expected)) return false;\n      continue;\n    }\n\n    const actual = String(input.value ?? \'\').trim();\n    if (finite(expected)) {\n      if (!finite(actual) || Math.abs(Number(actual) - Number(expected)) > 1e-9) return false;\n    } else if (actual !== String(expected ?? \'\')) {\n      return false;\n    }\n  }\n  return true;\n}\n\nfunction mf293InferProfileFromLogic() {\n  for (const key of [\'conservative\', \'balanced\', \'aggressive\']) {\n    if (mf293PresetMatches(key)) return key;\n  }\n  return \'custom\';\n}\n\nfunction mf293SyncProfileSelection() {\n  const input = document.querySelector(\'[data-setting-key="profile"]\');\n  if (!input) return \'custom\';\n  const profile = mf293InferProfileFromLogic();\n  input.value = profile;\n  return profile;\n}\n\nfunction mf293ProfileLabel(profile) {\n  const key = String(profile || \'custom\').trim().toLowerCase();\n  return key.charAt(0).toUpperCase() + key.slice(1);\n}\n'
NEW_MARK_DIRTY='  const markDirty = () => {\n    MF293.dirty = true;\n\n    if (key !== \'profile\' && MF293_PROFILE_LOGIC_KEYS.includes(key)) {\n      mf293SyncProfileSelection();\n    }\n\n    const profileInput = document.querySelector(\'[data-setting-key="profile"]\');\n    const profile = profileInput?.value || \'custom\';\n    mf293Status(`${mf293ProfileLabel(profile)} · Unsaved`, \'dirty\');\n  };'
NEW_PROFILE_LISTENER='  document.querySelector(\'[data-setting-key="profile"]\')?.addEventListener(\'change\', event => {\n    const profile = String(event.currentTarget?.value || \'\').trim().toLowerCase();\n\n    if (profile === \'custom\') {\n      MF293.dirty = true;\n      mf293Status(\'Custom · Unsaved\', \'dirty\');\n      return;\n    }\n\n    mf293ApplyProfilePreset(profile);\n  });'
TEST_TEXT='import assert from \'node:assert/strict\';\nimport fs from \'node:fs\';\n\nimport {\n  ENTRY_ADMISSION_KEYS,\n  LOGIC_DECISION_KEYS,\n  PREOPEN_RPC_KEYS,\n  evaluateEntryAdmission,\n  evaluateSettingsAdmission,\n  evaluateSettingsGate\n} from \'../src/settings-gate.mjs\';\n\nimport {\n  defaultSettings,\n  normalizeSettings,\n  validateSettings\n} from \'../src/settings.mjs\';\n\nconst now=Date.now();\n\nconst overlap=(a,b)=>a.filter(x=>b.includes(x));\nassert.deepEqual(overlap(ENTRY_ADMISSION_KEYS,LOGIC_DECISION_KEYS),[]);\nassert.deepEqual(overlap(ENTRY_ADMISSION_KEYS,PREOPEN_RPC_KEYS),[]);\nassert.deepEqual(overlap(LOGIC_DECISION_KEYS,PREOPEN_RPC_KEYS),[]);\n\nfor(const key of [\n  \'minBuyPressure\',\n  \'requireFreshHolderSnapshot\',\n  \'requireWebsiteOrX\',\n  \'minScore\',\n  \'minConfidence\',\n  \'decisionFreshnessSec\'\n]){\n  assert.equal(\n    ENTRY_ADMISSION_KEYS.includes(key),\n    false,\n    `${key} is Logic and must not hide scanner cards`\n  );\n}\n\nfor(const key of [\n  \'maxSuspectedRiskyWalletsPct\',\n  \'maxInsidersPct\'\n]){\n  assert.equal(\n    PREOPEN_RPC_KEYS.includes(key),\n    true,\n    `${key} must remain final RPC policy`\n  );\n  assert.equal(ENTRY_ADMISSION_KEYS.includes(key),false);\n}\n\nconst allEntryOff={\n  ...defaultSettings(),\n\n  minLiquidityUsd:0,\n  minHolders:null,\n  maxHolders:null,\n  minTokenAgeMinutes:null,\n  maxTokenAgeMinutes:null,\n  minMarketCapUsd:null,\n  maxMarketCapUsd:null,\n  minBondingCurvePct:null,\n  maxBondingCurvePct:null,\n  minTotalFeesSol:null,\n  maxTotalFeesSol:null,\n  minVolume24hUsd:null,\n  maxVolume24hUsd:null,\n  minBuyTransactions:null,\n  maxBuyTransactions:null,\n  minSellTransactions:null,\n  maxSellTransactions:null,\n  minTotalTransactions:null,\n  maxTotalTransactions:null,\n  minTop10Pct:null,\n  maxTop10Pct:null,\n  minDeveloperPct:null,\n  maxDeveloperPct:null,\n  minBundlePct:null,\n  maxBundlePct:null,\n  minSniperPct:null,\n  maxSniperPct:null,\n\n  requireTwitter:false,\n  requireWebsite:false,\n  requireTelegram:false,\n  requireAnySocial:false,\n  includeKeywords:\'\',\n  excludeKeywords:\'\',\n  developerBlacklistWallets:[],\n\n  // Intentionally strict Logic. These MUST NOT affect visibility.\n  minBuyPressure:9,\n  minScore:99,\n  minConfidence:99,\n  requireFreshHolderSnapshot:true,\n  requireWebsiteOrX:true,\n\n  // FINAL RPC only.\n  maxSuspectedRiskyWalletsPct:1,\n  maxInsidersPct:1\n};\n\nconst rawPump={\n  mint:\'RawPump111111111111111111111111111111111\',\n  launchPlatform:\'pump\',\n  discoveredAt:now,\n  buyPressure:null,\n  holderFresh:false,\n  socialsKnown:false\n};\n\nconst openAdmission=evaluateEntryAdmission(rawPump,allEntryOff,{now});\nassert.equal(openAdmission.admitted,true);\nassert.equal(openAdmission.gates.some(g=>g.key===\'minBuyPressure\'),false);\nassert.equal(openAdmission.gates.some(g=>g.key===\'requireFreshHolderSnapshot\'),false);\nassert.equal(openAdmission.gates.some(g=>g.key===\'requireWebsiteOrX\'),false);\n\n// Full Logic still applies AFTER admission.\nconst fullGate=evaluateSettingsGate(rawPump,allEntryOff);\nassert.notEqual(fullGate.state,\'PASS\');\n\n// Global discovery/holder admission must also ignore Logic-only failures.\nconst globalAdmission=evaluateSettingsAdmission(\n  rawPump,\n  [{uid:\'u1\',version:1,settings:allEntryOff}],\n  {now,recheckMs:2000}\n);\nassert.equal(globalAdmission.allow,true);\n\nconst mcFiltered=evaluateEntryAdmission(\n  {...rawPump,marketCapUsd:3000},\n  {...allEntryOff,minMarketCapUsd:5000},\n  {now}\n);\nassert.equal(mcFiltered.admitted,false);\nassert.equal(\n  mcFiltered.failedGates.some(g=>g.key===\'minMarketCapUsd\'),\n  true\n);\n\nconst mcAdmitted=evaluateEntryAdmission(\n  {...rawPump,marketCapUsd:5001},\n  {...allEntryOff,minMarketCapUsd:5000},\n  {now}\n);\nassert.equal(mcAdmitted.admitted,true);\n\nconst holderWaiting=evaluateEntryAdmission(\n  {...rawPump,marketCapUsd:6000,holderCount:null},\n  {...allEntryOff,minMarketCapUsd:5000,minHolders:30},\n  {now}\n);\nassert.equal(holderWaiting.admitted,false);\nassert.equal(holderWaiting.waitingGates.some(g=>g.key===\'minHolders\'),true);\n\nconst holderAdmitted=evaluateEntryAdmission(\n  {...rawPump,marketCapUsd:6000,holderCount:31},\n  {...allEntryOff,minMarketCapUsd:5000,minHolders:30},\n  {now}\n);\nassert.equal(holderAdmitted.admitted,true);\n\nconst rpcDoesNotHide=evaluateEntryAdmission(\n  {\n    ...rawPump,\n    marketCapUsd:6000,\n    suspectedRiskyWalletsPct:99,\n    insidersPct:99\n  },\n  {\n    ...allEntryOff,\n    minMarketCapUsd:5000,\n    maxSuspectedRiskyWalletsPct:1,\n    maxInsidersPct:1\n  },\n  {now}\n);\nassert.equal(rpcDoesNotHide.admitted,true);\n\n// "Custom" is a valid decision preset state, not a second rule layer.\nconst custom=validateSettings({...defaultSettings(),profile:\'custom\'});\nassert.equal(custom.ok,true);\nassert.equal(custom.settings.profile,\'custom\');\n\n// Obsolete UI-only controls must not survive canonical normalization.\nconst normalized=normalizeSettings({\n  ...defaultSettings(),\n  profile:\'custom\',\n  ownerApproval:true,\n  requireTokenLogo:true,\n  requireDevMigrated:true,\n  maxDeveloperRugHistoryPct:12,\n  maxDeveloperExitPct:15\n});\nfor(const key of [\n  \'ownerApproval\',\n  \'requireTokenLogo\',\n  \'requireDevMigrated\',\n  \'maxDeveloperRugHistoryPct\',\n  \'maxDeveloperExitPct\'\n]){\n  assert.equal(\n    Object.prototype.hasOwnProperty.call(normalized,key),\n    false,\n    `${key} must not exist in canonical settings`\n  );\n}\n\n// UI contract: no fake controls; wallet risk is separated from Entry Filters.\nconst settingsPage=fs.readFileSync(\n  new URL(\'../settings-page.js\',import.meta.url),\n  \'utf8\'\n);\n\nfor(const obsolete of [\n  "[\'ownerApproval\'",\n  "[\'requireTokenLogo\'",\n  "[\'requireDevMigrated\'",\n  "[\'maxDeveloperRugHistoryPct\'",\n  "[\'maxDeveloperExitPct\'"\n]){\n  assert.equal(settingsPage.includes(obsolete),false,obsolete);\n}\n\nassert.match(settingsPage,/\\[\'preopen\', \'Pre-open RPC verification\'/);\nassert.match(settingsPage,/\\[\'profile\', \'Decision preset\'.*?\'custom\',\'Custom\'/s);\nassert.match(settingsPage,/Minimum buy pressure for BUY READY/);\nassert.match(settingsPage,/Scanner admission only/);\nassert.match(settingsPage,/mf293SyncProfileSelection/);\n\nconst filterStart=settingsPage.indexOf("[\'filters\', \'Entry filters\'");\nconst preopenStart=settingsPage.indexOf("[\'preopen\', \'Pre-open RPC verification\'");\nassert.ok(filterStart>=0&&preopenStart>filterStart);\nconst filterBlock=settingsPage.slice(filterStart,preopenStart);\nassert.equal(filterBlock.includes(\'maxSuspectedRiskyWalletsPct\'),false);\nassert.equal(filterBlock.includes(\'maxInsidersPct\'),false);\n\nconst html=fs.readFileSync(\n  new URL(\'../settings.html\',import.meta.url),\n  \'utf8\'\n);\nassert.match(html,/settings-page\\.js\\?v=settings-architecture-v2/);\n\nconsole.log(\'settings architecture v2 ok\');\n'

try:
    gate=originals['gate']; settings=originals['settings']; page=originals['page']; html=originals['html']; pkg=originals['pkg']
    app_text=paths['app'].read_text(encoding='utf-8')
    if MARK in gate or MARK in page:
        print('Patch is already installed.'); raise SystemExit(0)
    for required in ('MEMEFLOW_STRICT_ENTRY_ADMISSION_V1','MEMEFLOW_OPPORTUNITY_ENGINE_V1','MEMEFLOW_FEED_RELEVANCE_RANKING_V1'):
        if required not in app_text and required not in gate:
            raise RuntimeError(f'Current main is missing required architecture marker: {required}')

    gate=sub_once(gate,r"export function evaluateSettingsAdmission\(token=\{\},entries=\[\],options=\{\}\)\{.*?\n\}\n\n// MEMEFLOW_STRICT_ENTRY_ADMISSION_V1",NEW_SETTINGS_ADMISSION+'\n\n// MEMEFLOW_STRICT_ENTRY_ADMISSION_V1','gate/global-entry-admission')
    gate=sub_once(gate,r"// MEMEFLOW_STRICT_ENTRY_ADMISSION_V1[\s\S]*$",NEW_GATE_BLOCK.strip()+'\n','gate/three-layer-policy')

    settings=replace_once(settings,"const VALID_MODES=['observe','assist','automate'],VALID_ENVS=['paper','live'],VALID_PROFILES=['conservative','balanced','aggressive'];","const VALID_MODES=['observe','assist','automate'],VALID_ENVS=['paper','live'],VALID_PROFILES=['conservative','balanced','aggressive','custom'];",'settings/custom-profile')
    settings=replace_once(settings,"delete o.paperBeforeChange;delete o.auditSettings;delete o.decisionFreshness;delete o.exitWeakPressure;delete o.ownerApproval;","delete o.paperBeforeChange;delete o.auditSettings;delete o.decisionFreshness;delete o.exitWeakPressure;delete o.ownerApproval;\n delete o.requireTokenLogo;delete o.requireDevMigrated;delete o.maxDeveloperRugHistoryPct;delete o.maxDeveloperExitPct;",'settings/remove-fake-controls')

    page=sub_once(page,r"const MF293_GROUPS = \[.*?\n\];\n\n/\*\n \* Strategy profiles",NEW_GROUPS+'\n\n/*\n * Strategy profiles','page/settings-groups')
    page=replace_once(page,"function mf293Fields() {\n  return MF293_GROUPS.flatMap(group => group[4]);\n}\n","function mf293Fields() {\n  return MF293_GROUPS.flatMap(group => group[4]);\n}\n"+PROFILE_HELPERS,'page/profile-inference')
    page=replace_once(page,"  const markDirty = () => {\n    MF293.dirty = true;\n    mf293Status('Unsaved', 'dirty');\n  };",NEW_MARK_DIRTY,'page/custom-on-manual-change')
    page=replace_once(page,"  document.querySelector('[data-setting-key=\"profile\"]')?.addEventListener('change', event => {\n    mf293ApplyProfilePreset(event.currentTarget?.value);\n  });",NEW_PROFILE_LISTENER,'page/profile-selector')
    page=replace_once(page,"  const environment = document.querySelector('[data-setting-key=\"tradingEnvironment\"]');","  mf293SyncProfileSelection();\n\n  const environment = document.querySelector('[data-setting-key=\"tradingEnvironment\"]');",'page/profile-sync-on-load')
    page=replace_once(page,"  MF293.dirty = true;\n  mf293Status(`${key.charAt(0).toUpperCase()}${key.slice(1)} · Unsaved`, 'dirty');\n  return true;","  mf293SyncProfileSelection();\n  MF293.dirty = true;\n  mf293Status(`${mf293ProfileLabel(key)} · Unsaved`, 'dirty');\n  return true;",'page/profile-preset-status')

    html=replace_once(html,'/settings-page.js?v=standalone-settings-v1','/settings-page.js?v=settings-architecture-v2','html/cache-bust')
    pkg=replace_once(pkg,'\"test\": \"node tests/strict-entry-admission.mjs &&','\"test\": \"node tests/settings-architecture-v2.mjs && node tests/strict-entry-admission.mjs &&','package/add-architecture-test')

    paths['test'].write_text(TEST_TEXT,encoding='utf-8')
    paths['gate'].write_text(gate.rstrip()+'\n',encoding='utf-8')
    paths['settings'].write_text(settings.rstrip()+'\n',encoding='utf-8')
    paths['page'].write_text(page.rstrip()+'\n',encoding='utf-8')
    paths['html'].write_text(html.rstrip()+'\n',encoding='utf-8')
    paths['pkg'].write_text(pkg.rstrip()+'\n',encoding='utf-8')

    print('=== Syntax checks ===')
    for p in (paths['gate'],paths['settings'],paths['page'],paths['test'],paths['app']):
        run(['node','--check',str(p)],cwd=root)
    print('=== Settings architecture regression ===')
    run(['node','tests/settings-architecture-v2.mjs'],cwd=app)
    print('=== Full test suite ===')
    run(['npm','test'],cwd=app)
    print('=== 500-user/performance benchmark ===')
    run(['npm','run','benchmark'],cwd=app)
    print('=== Diff validation ===')
    run(['git','diff','--check'],cwd=root)

except BaseException as error:
    print(f'ERROR: {error}')
    print('Rolling back local patch changes...')
    for k in touch_keys: paths[k].write_text(originals[k],encoding='utf-8')
    if test_existed: paths['test'].write_text(test_original,encoding='utf-8')
    else:
        try: paths['test'].unlink()
        except FileNotFoundError: pass
    print('Rollback complete. No commit/push was made.')
    raise

print('=== Commit + push ===')
changed=[paths[k] for k in touch_keys]+[paths['test']]
rel=[str(p.relative_to(root)) for p in changed]
run(['git','add','--',*rel],cwd=root)
run(['git','commit','-m','[MEMEFLOW_SETTINGS_ARCHITECTURE_V2] Separate entry, decision and pre-open risk settings'],cwd=root)
run(['git','push','origin','HEAD'],cwd=root)
print()
print('='*64)
print(' MEMEFLOW_SETTINGS_ARCHITECTURE_V2 INSTALLED SUCCESSFULLY')
print('='*64)
print('Restart the Replit backend/deployment.')
print()
print('Final policy pipeline:')
print(' RAW Pump WS')
print('   -> Entry Filters only (visibility / pre-admission)')
print('   -> visible Live Token States')
print('   -> Logic + Quality + Opportunity')
print('   -> WAITING / WATCH / BUY READY')
print('   -> Pre-open linked/funded-wallet Solana RPC')
print('   -> fresh WS re-check')
print('   -> OPEN POSITION')
print()
print('Removed fake UI controls and ownerApproval.')
print('Profile is now a preset; manual Logic edits become Custom.')
print('Logic settings can no longer hide otherwise admitted scanner cards.')
