MEMEFLOW V73 Photoreal Time-of-Day Scenes

1. Upload/extract this folder into the repository root.
2. Run:
   bash memeflow_v73_photoreal_scenes/install-v73.sh
3. Refresh Safari.

This preserves the V72 time/location controller and replaces only the embedded SVG backgrounds with three photorealistic WebP assets.
