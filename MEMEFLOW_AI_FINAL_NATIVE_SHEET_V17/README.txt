MEMEFLOW FINAL AI NATIVE SHEET v17

Analyze token inside MEMEFLOW OpenAI:
- stays on the OpenAI Assistant page
- runs the same MANUAL AI SCAN evaluator in the background
- does not navigate or scroll to Home
- does not use OpenAI chat credits for Analyze token
- mirrors the resulting MEMEFLOW analysis into the same OpenAI Assistant page

Install:
cd ~/workspace
unzip -o MEMEFLOW_AI_FINAL_NATIVE_SHEET_V17.zip
node MEMEFLOW_AI_FINAL_NATIVE_SHEET_V17/apply-ai-final-v17.mjs

Then:
Stop -> Run -> refresh Safari.

Rollback:
cd ~/workspace
node MEMEFLOW_AI_FINAL_NATIVE_SHEET_V17/rollback-final-v17.mjs
